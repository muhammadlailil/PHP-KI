<?php

     $a = 1;
     $b = 1;

     // echo ++$a; // pre increment
     // echo $b++; // post increment

     // echo --$a; // pre decrement
     // echo $b--; // post decrement


     // var_dump( $a == $b ); // equal (sama dengan)
     // var_dump( $a === $b ); // equal identikal (sama persis)
     // var_dump( $a != $b ); // not equal (tidak sama dengan) 1
     // var_dump( $a <> $b ); // not equal (tidak sama dengan) 2
     // var_dump( $a !== $b ); // not equal identikal (tidak sama dengan persis)

     // var_dump($a > $b);  // greater then
     // var_dump( $a < $b); // less then
     // var_dump( $a >= $b ); // greater then or equal
     // var_dump( $a <= $b); // less then or equal
?>